# 更新日志

## 2.0.0 - 2026-08-28

### 新增

- 插件迁移到 Runtime v2 format v3；WebView 由统一 `ui/main.json` 声明式文档选择，所有系统操作只调用 `accessibility.manage` 高层能力。
- 收藏与自动授权设置改由宿主 Dataset generation 管理，可原子备份、恢复和回滚。
- 增加不依赖常驻 WebView 的 JavaScript worker；应用进入前台及周期调度时可检查并启用收藏服务。

### 优化

- Web 界面直接消费宿主注入的主题、语义色、间距和圆角 token，保持浅色、深色和动态配色一致。
- 无障碍能力由合并 UI 的全信任 `shizuku_auth` 插件提供；普通 Tool 无法携带或绕过原生实现。
- 重排窄屏页面、搜索与自动启用区域及服务卡片，消除横向溢出，并改用与共享设计系统一致的状态色和用户文案。
- 去除与宿主顶栏重复的页面标题，按宿主下发的 Compose 字号、行高、字重和卡片间距重新排版，并将刷新、收藏、连接状态及服务类型统一为 Material 图标。
- 窄屏仍将“仅看收藏”“自动启用收藏”和开关保持在同一行，恢复原界面的操作归属。
- 主页小部件不再被重复的“已就绪”标签挤压，标题保持单行显示，并保留普通插件原有的浅色背景。
- 新增主页小部件，直接显示设备上的无障碍服务数量和当前启用数量。

### 修复

- 启用或停用后等待系统状态稳定并回读结果，不再把被系统安全策略撤销的操作误报为成功；自动任务会继续处理其他收藏并记录未能启用的项目。
- 收藏开启自动启用后立即尝试启用；旧版收藏和自动启用设置由宿主在同 ID 升级时先校验后导入。

### 兼容性

- 最低宿主版本提升到 Runtime v2 基线 23；纯 Web/Worker 包不再编译插件 SDK 或生成旧 API1 载荷。
- 数据写入版本提升到 v2；现有 v1 `accessibility-settings` 通过宿主迁移流程单向恢复到新的 Dataset generation。

### 架构

- 删除冻结期 API1 Android/Compose 实现、空 APK 工程和旧 fixture，仓库只保留 format v3 的 `src/` 清单、声明式 UI、Web 资源与 Worker。

## 1.4.1 - 2026-08-21

### 新增

- 正式提供无障碍收藏与自动授权设置的独立导入、导出和删除；导入支持替换，以及保留收藏并集的合并模式。

### 修复

- 冷启动后首次进入插件会立即读取连接与服务状态，不再需要退出、重进或手动刷新。

### 兼容性

- 基于正式插件 SDK `1.3.1` 构建，最低宿主版本提升到 22；本版本是 Bridge 预发布线验证后的正式版本。

## 1.4.0-bridge.5 - 2026-08-21

### 优化

- 重新基于插件 SDK `1.3.0-bridge.5` 构建，跟随宿主完整的浅色／深色容器色阶，避免共享界面回退到默认蓝紫色。

### 修复

- 插件在冷启动后首次进入时会在界面创建完成后主动读取连接与服务状态，不再需要退出、重进或等待额外刷新才显示内容。

### 兼容性

- 最低宿主版本提升到 21。

## 1.4.0-bridge.4 - 2026-08-21

### 新增

- 数据导入会报告当前是否已有无障碍设置，并支持由用户分别选择替换或合并。
- 合并会保留两侧收藏项的并集，并以导入包中的自动授权开关为最终值；替换则先清空旧偏好再写入完整数据。

### 兼容性

- 编译基线更新为临时插件 SDK `1.3.0-bridge.4`，最低宿主版本提升到 20。

## 1.4.0-bridge.3 - 2026-08-20

### 新增

- 数据管理支持独立删除无障碍收藏与自动授权设置，并在提交后验证插件偏好已清空。

### 兼容性

- 编译基线更新为临时插件 SDK `1.3.0-bridge.3`，最低宿主版本提升到 19；该版本仅投放 Debug 渠道。

## 1.4.0-bridge.2 - 2026-08-14

### 新增

- Migration Bridge 支持从 Dataset v1 恢复收藏列表与自动授权设置，并在提交后核对完整状态。

### 兼容性

- 编译基线更新为临时插件 SDK `1.3.0-bridge.2`，最低宿主版本提升到 18；该版本仅投放 Debug 渠道。

## 1.4.0-bridge.1 - 2026-08-14

### 新增

- Debug 预发布接入只读 Migration Bridge，可将收藏列表与自动授权设置导出为独立 Dataset。

### 兼容性

- 编译基线更新为临时插件 SDK `1.3.0-bridge.1`，最低宿主版本提升到 17；该版本仅投放 Debug 渠道。

## 1.3.1 - 2026-08-12

### 修复

- 服务状态标签改用共享状态组件，启用状态的绿色不再固定为浅色值，深色主题下对比度达标。

### 优化

- 间距与圆角改用共享设计系统的取值。
- 连接状态与错误提示不再出现内部术语「宿主」，未初始化时给出可操作的提示而不是内部错误信息。

### 兼容性

- 编译基线同步到 Android Tool Suite 插件 SDK 1.2.0，继续复用应用级主题偏好与共享设计系统。
- 数据兼容声明扩展为可读 `v0–v1`，旧安装记录及未声明版本留下的收藏和自动授权设置不会阻止重新安装或版本切换。

## 1.3.0 - 2026-08-07

### 新增

- `main` 分支 CI 成功后自动更新公开的 `debug` 预发布，供宿主调试仓库发现和安装。

### 优化

- 正式 Release 与调试预发布共用同一套元数据校验，并在发布完成后以事件通知插件索引重建。

## 1.2.0 - 2026-07-24

### 新增

- 接入官方插件仓库与 GitHub Release 自动发布流程。

### 兼容性

- 插件包升级为 format v2，声明整数版本号、最低宿主版本 11 和插件 SDK 1.1.0。

## 1.1 - 2026-07-13

### 优化

- 将插件页面从传统 Android View 重写为 Compose Material 3 界面，与主体应用和 Phigros Data Studio 使用同一套主题、动态配色和排版体系。
- 重新组织无障碍授权页的信息层级，使安全提示、连接状态、搜索筛选、自动化选项和服务列表更易浏览。
- 重写主页小部件，统一使用共享 `SuiteCard`、主题色、图标和文字层级。
- 使用共享设计系统统一卡片、按钮、输入框、筛选标签、状态标签以及空态和错误提示的视觉样式。
- 在服务列表标题中展示服务总数和已启用数量，并提供更清晰的 Shizuku 连接状态反馈。

## 1.0

### 新增

- 支持通过 Shizuku 查看、启用和停用设备上的无障碍服务。
- 支持收藏服务、仅查看收藏以及启动时自动启用收藏服务。
