# 更新日志

## 1.3.1 - 2026-08-12

### 修复

- 服务状态标签改用共享状态组件，启用状态的绿色不再固定为浅色值，深色主题下对比度达标。

### 优化

- 间距与圆角改用共享设计系统的取值。
- 连接状态与错误提示不再出现内部术语「宿主」，未初始化时给出可操作的提示而不是内部错误信息。

### 兼容性

- 编译基线同步到 Android Tool Suite 插件 SDK 1.2.0，继续复用应用级主题偏好与共享设计系统。
- 数据兼容声明扩展为可读 `v0–v1`，旧安装记录及未声明版本留下的收藏和自动授权设置不会阻止重新安装或版本切换。

## 1.3.0 - 2026-08-07

### 新增

- `main` 分支 CI 成功后自动更新公开的 `debug` 预发布，供宿主调试仓库发现和安装。

### 优化

- 正式 Release 与调试预发布共用同一套元数据校验，并在发布完成后以事件通知插件索引重建。

## 1.2.0 - 2026-07-24

### 新增

- 接入官方插件仓库与 GitHub Release 自动发布流程。

### 兼容性

- 插件包升级为 format v2，声明整数版本号、最低宿主版本 11 和插件 SDK 1.1.0。

## 1.1 - 2026-07-13

### 优化

- 将插件页面从传统 Android View 重写为 Compose Material 3 界面，与主体应用和 Phigros Data Studio 使用同一套主题、动态配色和排版体系。
- 重新组织无障碍授权页的信息层级，使安全提示、连接状态、搜索筛选、自动化选项和服务列表更易浏览。
- 重写主页小部件，统一使用共享 `SuiteCard`、主题色、图标和文字层级。
- 使用共享设计系统统一卡片、按钮、输入框、筛选标签、状态标签以及空态和错误提示的视觉样式。
- 在服务列表标题中展示服务总数和已启用数量，并提供更清晰的 Shizuku 连接状态反馈。

## 1.0

### 新增

- 支持通过 Shizuku 查看、启用和停用设备上的无障碍服务。
- 支持收藏服务、仅查看收藏以及启动时自动启用收藏服务。
